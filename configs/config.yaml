server:
  mode: "release"
  host: "0.0.0.0"
  port: 8080
  read_timeout: 30s
  write_timeout: 30s

log:
  level: "info"
  format: "console"
  path: "logs/changate"
  multi_files: false

etcd:
  endpoints:
    - "http://etcd:2379"
  timeout: 5s
  root_path: "/changate"
